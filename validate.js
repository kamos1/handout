export default class validation {
  static isWin(type) {
    let outcome_type_id
    return type === 'win' ?  outcome_type_id = 1 : outcome_type_id = 2;
  }

  static isWins(type) {
    let outcome_type_id
    return type === 'win' ?  outcome_type_id = 1 : outcome_type_id = 2;
  }
}
